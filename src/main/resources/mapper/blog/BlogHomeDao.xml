<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.dbc.robserver.blog.home.dao.BlogHomeDao">
    <!-- 获取用户及其关注的用户的博客 -->
    <select id="getBlogList" resultType="com.xjtu.dbc.robserver.common.model.article.Article">
        select *
        from user_follow_article
        <where>
            <if test="userId != null"> userid = #{userId} </if>
            <if test="articleType != null"> articletype = #{articleType} </if>
            and articlestatus = 402
        </where>
        ORDER BY createtime DESC;
    </select>
</mapper>