<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.dbc.robserver.blog.report.dao.BlogReportDao">
    <insert id="insertReport">
        INSERT INTO report(submittime, reason, reportstatus, userid, articleid)
        VALUES (#{submittime}, #{reason}, #{reportstatus}, #{userid}, #{articleid});
    </insert>
</mapper>